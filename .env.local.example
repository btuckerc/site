# Copy this file to .env.local and add your GitHub token
# Get a token at: https://github.com/settings/tokens
# Only needs 'public_repo' scope (or no scopes if repos are public)

VITE_GITHUB_TOKEN=your_github_token_here
